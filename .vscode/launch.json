{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Python: Django",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": [
                "runserver"
            ],
            "django": true,
            "justMyCode": true,
            "envFile": "${workspaceFolder}/dev.env",
            "preLaunchTask": "migrate"
        },

    ]
}
