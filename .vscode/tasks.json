{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "migrate",
            "command": "set -a && . ${workspaceFolder}/dev.env && python manage.py migrate",
        }
    ]
}
